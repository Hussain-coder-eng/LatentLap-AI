# Ralph Progress Log
Started: Sun Jun  7 21:58:25 EDT 2026

## Codebase Patterns
- `useNarrowViewport()` hook in `dashboard/lib/useNarrowViewport.ts` — returns boolean for < 640px, use for mobile branches in components
- Scrubber (`LapScrubberFixed`) is `position: fixed; bottom: 0; height: 48px; zIndex: 50` — chapter nav rail must sit at `bottom: 56px` minimum to clear it
- All dashboard components are `'use client'` and live in `dashboard/app/components/`
- McLaren orange accent: `#FF8000`; dark bg: `rgba(8,8,8,0.x)`; code font: `'Fira Code', monospace`
- Typecheck: `cd dashboard && npx tsc --noEmit`

---

## 2026-06-07 22:00 - US-001
- Rewrote ChapterDots.tsx: desktop → horizontal dot rail at `bottom: 56px` centered, inside a pill-shaped backdrop container; mobile → full-width segmented control at `top: 0` with abbreviated labels and orange underline indicator
- Added `title` attribute to each dot button for native hover tooltip
- Added `role="tablist"` / `role="tab"` / `aria-selected` for accessibility
- Files changed: `dashboard/app/components/ChapterDots.tsx`
- **Learnings for future iterations:**
  - Scrubber height is 48px at bottom: 0; rail goes at bottom: 56px to avoid overlap
  - `useNarrowViewport` is the correct hook for mobile branching — returns false on SSR (avoids hydration flash)
  - Short names array needed separately from full names for mobile segmented control
---

## 2026-06-07 22:10 - US-002
- Pushed Speedometer container from `top: 24` to `top: 52` so driver/year chip (SettingsPopover pill at top:16, height ~21px) no longer overlaps the gauge
- Added `SEVERITY_LEGEND` constant and rendered a compact swatch+label column below the confidence bar — always visible, not data-gated
- Files changed: `dashboard/app/components/Speedometer.tsx`
- **Learnings for future iterations:**
  - Driver/year chip lives in `SettingsPopover.tsx` at `position: fixed, top:16, left:16, zIndex:300` — it's the pill button that also opens the popover
  - Speedometer sits at `position: fixed, top:52, left:24, zIndex:60` after this fix — below chip's bottom edge (~37px)
  - `SEVERITY_PROB_COLORS` in Speedometer.tsx already had the 4 severity colors; legend reuses same array
---

## 2026-06-07 22:15 - US-003
- Changed confidence bar title from "CONFIDENCE" to "Severity probability"
- Increased bar height from 10px to 18px to fit label text
- Switched from fixed `width: prob * 120` to `flex: prob` layout so all 4 segments always render proportionally
- Added `title` and `aria-label` on each segment for accessibility when label is hidden
- Show "LabelName XX%" text when prob >= 0.12; hide visually (but keep aria) below threshold
- Files changed: `dashboard/app/components/Speedometer.tsx`
- **Learnings for future iterations:**
  - `flex: prob` is cleaner than `width: prob * 120` for proportional bars — handles float sums correctly
  - Confidence bar and severity legend are both in `Speedometer.tsx` — single file for all gauge-area UI
---

## 2026-06-08 03:39 - US-004
- Fixed CornerHeatmap: title "Corner Stress by Lap", corner label font bumped to 10px, gradient legend row with "Low stress" / "High stress" labels and proxy note
- Fixed ScrollStage: replaced `position: absolute, bottom: 20, left: 50%` with `position: fixed, bottom: 48px, left: 0, right: 0, height: 120px` — dark bg, orange top border, zIndex: 40 (below scrubber at 50)
- Files changed: `dashboard/app/components/CornerHeatmap.tsx`, `dashboard/app/components/ScrollStage.tsx`
- **Learnings for future iterations:**
  - CornerHeatmap panel sits at `position: fixed; bottom: 48px; height: 120px; zIndex: 40` — below scrubber (zIndex 50) and above content
  - Gradient CSS for the legend: `linear-gradient(to right, rgb(13,17,23), rgb(80,10,5), rgb(255,24,1))` matches `interpolateColor()` function range
  - When moving overlays to fixed panels, ensure zIndex is below scrubber (50) and above main content (30–39)
---
