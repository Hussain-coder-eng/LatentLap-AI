# Ralph Progress Log
## Codebase Patterns
- shap_data.json import from dashboard/app/components/: use '../../public/data/shap_data.json'
- New components go in dashboard/app/components/ as 'use client' with named export default
- Chapter-specific UI rendered via {activeChapter === N && (...)} blocks inside ScrollStage pinRef div
- TypeScript check: cd dashboard && npx tsc --noEmit

Started: Sun Jun  7 21:18:15 EDT 2026
---

## 2026-06-07 21:30 - US-001
- Implemented CornerHeatmap.tsx: div-based grid showing per-corner SHAP stress across laps
- Component: 4 corners (Copse, MB, Stowe, Club) x N laps, color lerp from near-black to red
- Integrated into ScrollStage.tsx chapter 2 (Predictors), positioned bottom-center
- Files changed: dashboard/app/components/CornerHeatmap.tsx (new), dashboard/app/components/ScrollStage.tsx (import + chapter 2 block)
- **Learnings for future iterations:**
  - shap_data.json already loaded via lib/data.ts for getSHAP(); importing it directly in a component also works fine
  - Corner prefix filtering: features like 'Copse_EntrySpeed' match 'Copse_' prefix; all 4 corners present for all year/driver combos
  - x-axis label positioning uses position:absolute inside a flex container with a 40px spacer matching corner label width (36px + 4px margin)
  - tsc --noEmit passes with no args needed beyond cd dashboard
---
